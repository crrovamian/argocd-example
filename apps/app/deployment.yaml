apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-app
  labels:
    app: nginx-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-app
  template:
    metadata:
      labels:
        app: nginx-app
    spec:
      containers:
      - name: nginx-app
        image: nginx:1.25
        ports:
        - containerPort: 80
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: VERSION
          value: "v1"
        command: ["/bin/sh", "/scripts/generate.sh"]
        volumeMounts:
        - name: html-volume
          mountPath: /usr/share/nginx/html
        - name: scripts
          mountPath: /scripts
        resources:
          limits:
            cpu: 500m
            memory: 256Mi
          requests:
            cpu: 200m
            memory: 128Mi
      volumes:
      - name: html-volume
        emptyDir: {}
      - name: scripts
        configMap:
          name: nginx-app-html
          defaultMode: 0777
